services:
  starlight-docs:
    build:
      context: .
      args:
        - URL_REPO_DOCS=${URL_REPO_DOCS} # Pasa la variable del host al contenedor al construir
    container_name: starlight
    restart: unless-stopped
    ports:
      - "8080:8080"